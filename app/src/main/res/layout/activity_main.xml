<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/main_activity_gradient"
    tools:context=".MainActivity">

    <EditText
        android:id="@+id/edit_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/margin_left_view"
        android:layout_marginEnd="@dimen/margin_right_view"
        android:hint="@string/hint_edit_text_view" />

    <Button
        android:id="@+id/button_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/enter_button"
        android:layout_marginStart="@dimen/margin_left_view"
        android:layout_marginEnd="@dimen/margin_right_view"
        android:layout_below="@id/edit_text_view"/>

</RelativeLayout>